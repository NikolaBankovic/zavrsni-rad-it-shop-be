spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/itshop-db
    username: itshop
    password: S3cret
  jpa:
    hibernate:
      ddl-auto: update
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB